version: '2'
services:
  postgres:
    image: "fifa-app-database"
    ports:
    - "5432"
    environment:
    - POSTGRES_PASSWORD=postgres
    volumes:
    - data:/var/lib/postgres
  fifa-app-api:
    image: "fifa-app-api"
    ports:
    - "3002:3002"
volumes:
  data:
    driver: "local"
